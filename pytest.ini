[pytest]
testpaths = tests
# The repo root is itself a Python package (__init__.py with relative
# imports). importlib mode keeps pytest from importing it during collection.
addopts = --import-mode=importlib
